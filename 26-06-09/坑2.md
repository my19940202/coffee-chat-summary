我再把第二个坑的一些详细细节给列给说一下，就是当时项目管理员希望开发环境，跟这个真实的这个上线部署环境给它区分开。那我一开始就是参照AI的建议，那他是怎么做的呢？他是在这个Cloudflare这个ra.jasonjasonc这个文件里面搞了两个环境，就en v有两个，一个是production，一个是EV。我用这种方式去试，其实是不行的，应该是应该说一个walker里面它只部署一份代码，应该是两种方式，应该是不OK的。那我想到的处理方案就是说，我把我搞两个，那第一个就是正式的这个分正式环境的分支，那我把它域名指向正式分支，然后用的也是main main分支或者master分支，这个完全没啥问题。那出问题就出问题的第二个，那我又新建了一个，那比如这个可能用用test分支，那我平时在test分支上面开发，开发完了我给它和master那这块过程中就遇到一些问题了。那问题是什么呢？首先，这个非主干分支开发的时候，那他去写那个构建命令，那构建命令其里面其实有一个非生产分支构建，那这个东西被我忽略掉了，然后我在那边一直折腾折腾什么呢？就是我折腾这个分支的部署，但是部署完之后一直是main分支，它部署的代码一直不是最新的，不是我预期的这个test分支，导致我在这段时间折腾了很久。那后面我又一轮借助AI，让他帮我去分析为什么这个代码不是最新的，那他帮我手动去修改了Cloudflare上面的这个构建命令，然后这会儿呢，有一个问题是解决了解决了什么问题呢？就解决了部署之后，代码就这个代码有没有真实到线上环境确实是到线上环境了。但是呢，它发布出来的环境呢，是那个commit那个哈希点一个什么worker.url，这是一个它每一次部署产生的一个临时的URU。它这个是产生了是达到效果了，但是呢，test的分支上面，我绑定的那个域名，它没有真实的，它那个代码还是老的，它访问过去是什么呢？它访问过去是什么？Hello, world, 这个是walker初始化之后的一个默认的一个内容，就一直死活搞不过去，那后面找到找到哪里呢？找到那个Cloudflare上面，它其实有一个配置，非生产分支构建，还是这里出问题了，它那边写的命令呢，是upload，upload只是说上传上去，它不是真的那个部署，然后最后怎么做的呢？就是这就是我test这个分支的那个walker，它的那个生产分支构建跟跟这个非生产分支构建写同样的命令，都是deploy，都是把这个代码给它部署过去。之前说到那个upload是什么，upload只是上传，它并没有把这个域名的流量给它完全切过来，主要是搞了这么个操作，然后让我test分支的每一次提交都能触发CICD部署，都能到这个呃test这个呃域名上面去  